{
  "tasks": [
    {
      "type": "cppbuild",
      "label": "build cpp",
      "command": "/usr/bin/g++",
      "args": [
        "-fdiagnostics-color=always",
        "-std=c++11",
        "-Wall",
        "-g",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}.out"
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "group": "build",
      "detail": "Task generated by Debugger."
    }
  ]
}
